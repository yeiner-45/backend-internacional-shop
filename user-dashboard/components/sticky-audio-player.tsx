"use client";
import { Pause, Play, SkipBack, SkipForward, Volume2 } from "lucide-react";
import { useAudioPlayer } from "@/context/audio-player";

export function StickyAudioPlayer() { const { track, playing, progress, volume, toggle, setProgress, setVolume } = useAudioPlayer(); return <aside className="fixed inset-x-0 bottom-0 z-50 border-t border-white/10 bg-[#141414]/95 backdrop-blur-md"><div className="mx-auto grid min-h-[82px] max-w-7xl grid-cols-[1fr_auto] items-center gap-4 px-4 py-3 md:grid-cols-[280px_1fr_220px] md:px-8"><div className="flex min-w-0 items-center gap-3"><div className="grid h-12 w-12 shrink-0 place-items-center bg-[#2a2a2a] text-[#ef233c]">{track ? <span className="text-lg font-black">IS</span> : <Play size={18}/>}</div><div className="min-w-0"><p className="truncate text-sm font-bold">{track?.title ?? "Selecciona una pista"}</p><p className="truncate text-xs text-zinc-400">{track?.artist ?? "Tu reproductor global está listo"}</p></div></div><div className="order-3 col-span-2 flex items-center gap-3 md:order-none md:col-span-1"><button className="hidden text-zinc-400 md:block" aria-label="Anterior"><SkipBack size={17}/></button><button onClick={toggle} disabled={!track} className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-[#ef233c] text-white disabled:bg-zinc-700" aria-label={playing ? "Pausar" : "Reproducir"}>{playing ? <Pause size={18} fill="currentColor"/> : <Play size={18} fill="currentColor"/>}</button><button className="hidden text-zinc-400 md:block" aria-label="Siguiente"><SkipForward size={17}/></button><input aria-label="Progreso de la pista" className="range h-1 w-full cursor-pointer" type="range" value={progress} onChange={e => setProgress(Number(e.target.value))}/></div><div className="flex items-center justify-end gap-2"><Volume2 size={17} className="text-zinc-400"/><input aria-label="Volumen" className="range h-1 w-20 cursor-pointer" type="range" min="0" max="1" step=".01" value={volume} onChange={e => setVolume(Number(e.target.value))}/></div></div></aside>; }
